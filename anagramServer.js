/*
Given a list of valid English words, build an anagram server that can spit out all the anagrams of a word.

(anagram = permutation that is also valid word)
rates = aster, stare, tears, tares, taser
dgo = dog, god
aeelps = please, elapse, asleep
*/

const anagramServer = (word, wordList) => {

};
